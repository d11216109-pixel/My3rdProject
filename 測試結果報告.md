# 單元測試執行結果報告

## 測試日期
2025-11-21

## 測試目的
展示單元測試的綠燈（通過）和紅燈（失敗）結果，驗證防呆機制的重要性。

---

## 🟢 綠燈測試結果（所有測試通過）

### 測試執行條件
- safe_division 函式包含完整的錯誤處理機制
- 使用 try-except 處理 ZeroDivisionError、TypeError 等異常

### 測試結果
```
Ran 16 tests in 0.001s
OK
```

### 通過的測試項目（16/16）
1. ✅ test_normal_division - 測試正常的除法運算
2. ✅ test_division_with_decimal_result - 測試產生小數結果的除法
3. ✅ test_division_with_float_numbers - 測試浮點數的除法
4. ✅ test_division_by_zero - 測試除以零的情況（**關鍵測試**）
5. ✅ test_negative_numbers - 測試負數的除法
6. ✅ test_division_with_zero_numerator - 測試分子為零的情況
7. ✅ test_division_with_one - 測試除以 1 的情況
8. ✅ test_division_with_large_numbers - 測試大數字的除法
9. ✅ test_division_with_small_numbers - 測試小數字的除法
10. ✅ test_type_error_string_numerator - 測試字串作為分子的錯誤處理
11. ✅ test_type_error_string_denominator - 測試字串作為分母的錯誤處理
12. ✅ test_type_error_none_values - 測試 None 值的錯誤處理
13. ✅ test_division_fractional_result - 測試產生分數結果的除法
14. ✅ test_boolean_values - 測試布林值
15. ✅ test_very_large_division - 測試非常大的數字除法
16. ✅ test_very_small_division - 測試非常小的數字除法

### 結論
所有測試通過，證明 safe_division 函式能正確處理各種情境，包含：
- 正常數值相除
- 負數相除
- 邊界值相除
- **除以零的防呆處理**（不會導致程式當機）

---

## 🔴 紅燈測試結果（測試失敗）

### 測試執行條件
- 將 safe_division 函式中的錯誤處理機制（try-except 區塊）註解或刪除
- 函式直接執行 `result = numerator / denominator` 而不捕捉異常

### 測試結果
```
Ran 16 tests in 0.002s
FAILED (errors=4)
```

### 失敗的測試項目（4/16）

#### 1. ❌ test_division_by_zero（除以零測試）
**錯誤訊息：**
```
ZeroDivisionError: division by zero
```

**失敗原因：**
當 b 為零時，safe_division 函式無法妥善處理除以零的情況，直接拋出 ZeroDivisionError 異常。由於沒有 try-except 機制捕捉此異常，程式執行時會中斷，測試無法通過。

**程式碼位置：**
```python
File "safe_division.py", line 27, in safe_division
    result = numerator / denominator
             ~~~~~~~~~~^~~~~~~~~~~~~
```

#### 2. ❌ test_type_error_string_numerator（字串分子測試）
**錯誤訊息：**
```
TypeError: unsupported operand type(s) for /: 'str' and 'int'
```

**失敗原因：**
當分子為字串時，Python 無法執行除法運算，拋出 TypeError。沒有錯誤處理機制時，程式直接崩潰。

#### 3. ❌ test_type_error_string_denominator（字串分母測試）
**錯誤訊息：**
```
TypeError: unsupported operand type(s) for /: 'int' and 'str'
```

**失敗原因：**
當分母為字串時，Python 無法執行除法運算，拋出 TypeError，導致程式崩潰。

#### 4. ❌ test_type_error_none_values（None 值測試）
**錯誤訊息：**
```
TypeError: unsupported operand type(s) for /: 'NoneType' and 'int'
```

**失敗原因：**
當參數為 None 時，Python 無法執行除法運算，拋出 TypeError，程式無法繼續執行。

### 通過的測試項目（12/16）
以下測試仍然通過，因為它們使用的是正常的數值：
- test_normal_division
- test_division_with_decimal_result
- test_division_with_float_numbers
- test_negative_numbers
- test_division_with_zero_numerator
- test_division_with_one
- test_division_with_large_numbers
- test_division_with_small_numbers
- test_division_fractional_result
- test_boolean_values
- test_very_large_division
- test_very_small_division

---

## 📊 測試結果比較

| 測試情境 | 有錯誤處理 | 無錯誤處理 |
|---------|-----------|-----------|
| 正常數值相除 | ✅ 通過 | ✅ 通過 |
| 除以零 | ✅ 通過 | ❌ 失敗 (ZeroDivisionError) |
| 字串參數 | ✅ 通過 | ❌ 失敗 (TypeError) |
| None 參數 | ✅ 通過 | ❌ 失敗 (TypeError) |
| 負數相除 | ✅ 通過 | ✅ 通過 |
| 邊界值測試 | ✅ 通過 | ✅ 通過 |
| **總計** | **16/16 (100%)** | **12/16 (75%)** |

---

## 🎯 結論與重要性

### 哪些測試失敗？
1. **test_division_by_zero**（除以零測試）
2. test_type_error_string_numerator（字串分子測試）
3. test_type_error_string_denominator（字串分母測試）
4. test_type_error_none_values（None 值測試）

### 失敗原因
**最關鍵的失敗是 test_division_by_zero**。原因是函式內部沒有處理除以零的例外（ZeroDivisionError），導致執行時拋出錯誤，測試無法通過。當使用者不小心輸入 0 作為除數時，程式會直接崩潰，而不是優雅地處理錯誤。

### 防呆機制的重要性
這個測試結果證明了防呆機制（error handling）的重要性：

1. **提升程式穩定性**：有錯誤處理時，即使遇到異常輸入，程式仍能繼續運行
2. **改善使用者體驗**：返回 None 並顯示友善的錯誤訊息，而不是讓程式崩潰
3. **便於除錯**：清楚的錯誤訊息幫助開發者和使用者理解問題所在
4. **提高程式碼品質**：單元測試確保所有邊界情況都被考慮和處理

### 最佳實踐
在編寫可能發生錯誤的程式碼時，應該：
- 使用 try-except 捕捉可預期的異常
- 提供清楚的錯誤訊息
- 返回適當的預設值（如 None）
- 編寫單元測試驗證錯誤處理機制

---

## 📝 測試程式碼範例

### 成功的錯誤處理
```python
def safe_division(numerator, denominator):
    try:
        result = numerator / denominator
        return result
    except ZeroDivisionError:
        print("錯誤：除數不能為零")
        return None
    except TypeError:
        print("錯誤：參數必須是數字")
        return None
```

### 沒有錯誤處理（會導致測試失敗）
```python
def safe_division(numerator, denominator):
    # 移除錯誤處理以展示測試失敗（紅燈）
    result = numerator / denominator
    return result
```

---

**報告完成日期：** 2025-11-21  
**測試工具：** Python unittest  
**測試框架版本：** Python 3.x
